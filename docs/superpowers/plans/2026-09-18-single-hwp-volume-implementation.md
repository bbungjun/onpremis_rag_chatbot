# 단일 HWP 규정집 구현 계획 (2026-09-18)

1. `제1조` 반복 시 현재 청크 ID가 충돌하는 사실을 확인하고, 단일 규정집의 전역 조 번호·문서 구조 테스트를 먼저 작성한다.
2. 1,000개 합성 정책을 편/장/절 순서로 합치는 `book` 생성 경로를 구현한다. 기존 `split` 생성 경로는 선택지로 유지한다.
3. 작은 실제 HWP 왕복을 검사한 뒤 단일 1,000개 정책 HWP를 생성하고 파일 크기, 해시, 조·항 수, 고유 ID와 샘플 렌더를 확인한다.
4. 사용자 안내, README, 포트폴리오 기록을 새로운 기본 산출물과 측정 결과로 갱신한다.
5. 관련 pytest, Docker 필수 명령, 파일별 diff, `git diff --check`를 확인하고 기존 PR을 갱신한다.
