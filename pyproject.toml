[tool.ruff]
line-length = 100
# Ruff >= 0.16 also formats code blocks in Markdown; design/plan docs are prose.
extend-exclude = ["*.md"]
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".", "src"]
